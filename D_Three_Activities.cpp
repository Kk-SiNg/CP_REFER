#include <bits/stdc++.h>
using namespace std;
#define ll long long

void solve(){
    int n, a;
    vector <int> vect_a, vect_b, vect_c;
    cin >> n;
    for(int i = 0; i < n; i++){
        cin >> a;
        vect_a.push_back(a);
    }
    for(int i = 0; i < n; i++){
        cin >> a;
        vect_b.push_back(a);
    }
    for(int i = 0; i < n; i++){
        cin >> a;
        vect_c.push_back(a);
    }

    
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t--){
        solve();
    }
}